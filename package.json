{
  "name": "stem-player",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "test": "node tests/regressions.js && node tests/upload-smoke.js",
    "test:regressions": "node tests/regressions.js",
    "test:smoke": "node tests/upload-smoke.js",
    "playwright:install": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.50.0"
  }
}
