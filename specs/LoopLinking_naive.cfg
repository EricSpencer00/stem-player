\* Naive (buggy) model: individual stem edits leave the All indicator stale.
\* TLC is expected to REPORT a violation of AllRowConsistent with a trace —
\* this is the malformed state the fix eliminates.
CONSTANTS
  Stems = {drums, bass, vocals, melody}
  Lengths = {1, 2}
  ClearAllOnStemEdit = FALSE

INIT Init
NEXT Next

INVARIANTS
  TypeOK
  AllRowConsistent
